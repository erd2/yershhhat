# Персональный сайт-профиль Toleubayev Yershat

Современный, адаптивный персональный сайт-портфолио разработчика с полным набором функций.

## 🚀 Особенности

### Frontend
- ✅ Современный дизайн в стиле минимализма
- ✅ Темная и светлая тема с переключателем
- ✅ Адаптивный дизайн для всех устройств
- ✅ Форма редактирования профиля с валидацией
- ✅ Плавные анимации и переходы
- ✅ Семантическая HTML5 разметка
- ✅ Оптимизация для SEO

### Backend API
- ✅ REST API для управления профилем
- ✅ CRUD операции (создание, чтение, обновление)
- ✅ SQLite база данных
- ✅ Валидация данных на сервере
- ✅ CORS поддержка
- ✅ Rate limiting
- ✅ Безопасность с Helmet.js
- ✅ Логирование запросов

### Функциональность
- ✅ Редактирование профиля в реальном времени
- ✅ Локальное сохранение данных
- ✅ Валидация форм на клиенте и сервере
- ✅ Интеграция с GitHub (готовность)
- ✅ Экспорт резюме в PDF/текст
- ✅ Система уведомлений
- ✅ Обработка ошибок

## 📁 Структура проекта

```
portfolio-website/
├── index.html              # Главная страница
├── styles.css              # Стили (темная/светлая тема)
├── script.js               # JavaScript функциональность
├── README.md               # Документация
└── backend/                # Backend API
    ├── server.js           # Express сервер
    ├── package.json        # Node.js зависимости
    ├── .env               # Конфигурация
    └── portfolio.db       # SQLite база данных
```

## 🛠 Установка и запуск

### Frontend (статический сайт)

1. **Клонируйте или скачайте файлы:**
   ```bash
   # Если используете git
   git clone <repository-url>
   cd portfolio-website
   
   # Или просто скачайте файлы напрямую
   ```

2. **Запустите локальный сервер:**
   ```bash
   # С помощью Python (если установлен)
   python -m http.server 8080
   
   # Или с помощью Node.js (если установлен npx)
   npx serve .
   
   # Или откройте index.html напрямую в браузере
   ```

3. **Откройте в браузере:**
   ```
   http://localhost:8080
   ```

### Backend API (Node.js)

1. **Перейдите в папку backend:**
   ```bash
   cd backend
   ```

2. **Установите зависимости:**
   ```bash
   npm install
   ```

3. **Настройте переменные окружения:**
   ```bash
   # Отредактируйте .env файл при необходимости
   nano .env
   ```

4. **Запустите сервер:**
   ```bash
   # Режим разработки
   npm run dev
   
   # Или продакшн режим
   npm start
   ```

5. **Проверьте работу API:**
   ```
   http://localhost:3001/api/health
   ```

## 📋 API Документация

### Профиль

#### GET /api/profile
Получить данные профиля

**Ответ:**
```json
{
  "success": true,
  "data": {
    "id": 1,
    "name": "Toleubayev Yershat",
    "bio": "Разработчик с фокусом на адаптивные решения...",
    "skills": ["Адаптивность"],
    "phone": "+77780958898",
    "github": "https://github.com",
    "projects": "Работа с языковыми моделями...",
    "experience": "В разработке и изучении...",
    "education": "Самообразование и практический опыт...",
    "created_at": "2024-01-01T00:00:00.000Z",
    "updated_at": "2024-01-01T00:00:00.000Z"
  }
}
```

#### POST /api/profile
Создать новый профиль

**Тело запроса:**
```json
{
  "name": "Toleubayev Yershat",
  "bio": "Описание профиля",
  "skills": ["Адаптивность", "JavaScript"],
  "phone": "+77780958898",
  "github": "https://github.com/username",
  "projects": "Описание проектов",
  "experience": "Описание опыта",
  "education": "Описание образования"
}
```

#### PUT /api/profile
Обновить профиль

### Контакты

#### POST /api/contact
Отправить сообщение через контактную форму

**Тело запроса:**
```json
{
  "name": "Имя отправителя",
  "email": "email@example.com",
  "message": "Текст сообщения"
}
```

### Система

#### GET /api/health
Проверка состояния API

## ⚙️ Настройка

### Изменение данных профиля

1. **Через веб-интерфейс:**
   - Нажмите кнопку "Редактировать" в правом нижнем углу
   - Заполните форму и сохраните изменения

2. **Через API:**
   ```bash
   curl -X PUT http://localhost:3001/api/profile \
     -H "Content-Type: application/json" \
     -d '{
       "name": "Ваше имя",
       "bio": "Ваше описание",
       "skills": ["Навык 1", "Навык 2"],
       "phone": "+70000000000",
       "github": "https://github.com/username"
     }'
   ```

### Настройка GitHub интеграции

1. Обновите `GITHUB_USERNAME` в `.env` файле
2. Добавьте `GITHUB_TOKEN` для доступа к приватным репозиториям (опционально)

### Настройка базы данных

База данных SQLite создается автоматически при первом запуске сервера. Для сброса данных:

1. Остановите сервер
2. Удалите файл `backend/portfolio.db`
3. Перезапустите сервер

## 🎨 Кастомизация дизайна

### Изменение цветовой схемы

Отредактируйте CSS переменные в файле `styles.css`:

```css
:root {
  --brand-blue: #3B82F6;          /* Основной цвет */
  --page-bg-dark: #0A0A0A;        /* Фон (темная тема) */
  --surface-bg-dark: #141414;     /* Поверхности (темная) */
  /* ... другие переменные */
}
```

### Добавление новых секций

1. Добавьте HTML разметку в `index.html`
2. Создайте соответствующие стили в `styles.css`
3. Добавьте JavaScript логику в `script.js` при необходимости

## 🔒 Безопасность

- ✅ CORS настроен для локальной разработки
- ✅ Rate limiting для предотвращения спама
- ✅ Валидация входных данных
- ✅ SQL injection защита (SQLite параметризованные запросы)
- ✅ Helmet.js для заголовков безопасности

**Важно:** В продакшене обязательно:
- Измените секретные ключи в `.env`
- Настройте правильные CORS домены
- Используйте HTTPS
- Регулярно обновляйте зависимости

## 📱 Мобильная версия

Сайт полностью адаптивен и оптимизирован для:
- 📱 Мобильных устройств (320px+)
- 📱 Планшетов (768px+)
- 💻 Десктопов (1024px+)

## 🚀 Развертывание

### Frontend (статический хостинг)

**GitHub Pages:**
1. Загрузите файлы в репозиторий
2. Включите GitHub Pages в настройках
3. Выберите главную ветку

**Netlify/Vercel:**
1. Подключите репозиторий
2. Настройте build команды (если нужно)
3. Деплой происходит автоматически

### Backend (Node.js хостинг)

**Heroku:**
```bash
heroku create your-portfolio-api
git subtree push --prefix backend heroku main
```

**Railway/Render:**
1. Подключите GitHub репозиторий
2. Укажите папку `backend` как рабочую директорию
3. Настройте переменные окружения

## 🔧 Технологии

### Frontend
- **HTML5** - Семантическая разметка
- **CSS3** - Современные стили и анимации
- **JavaScript ES6+** - Интерактивность
- **Responsive Design** - Адаптивность

### Backend
- **Node.js** - JavaScript runtime
- **Express.js** - Web framework
- **SQLite** - База данных
- **CORS** - Cross-Origin Resource Sharing
- **Helmet.js** - Безопасность

### Инструменты разработки
- **Nodemon** - Автоматическая перезагрузка
- **Express Validator** - Валидация данных
- **Morgan** - Логирование
- **Rate Limiting** - Ограничение запросов

## 🐛 Устранение неполадок

### Проблемы с запуском

**Порт уже используется:**
```bash
# Найдите процесс на порту 3001
lsof -ti:3001

# Убейте процесс
kill -9 <PID>
```

**Ошибки базы данных:**
```bash
# Проверьте права доступа к папке
chmod 755 backend/
```

### Проблемы с CORS

Если возникают ошибки CORS:
1. Проверьте настройки в `server.js`
2. Убедитесь, что frontend запущен на правильном порту
3. Обновите `FRONTEND_URL` в `.env`

### Проблемы с производительностью

1. Включите сжатие статических файлов
2. Оптимизируйте изображения
3. Используйте CDN для статических ресурсов

## 📞 Поддержка

Для вопросов и предложений:
- 📧 Email: contact@example.com
- 💬 GitHub Issues: [Создать Issue](https://github.com/username/portfolio/issues)

## 📄 Лицензия

Этот проект распространяется под лицензией MIT. Подробности в файле LICENSE.

---

**Автор:** Toleubayev Yershat  
**Версия:** 1.0.0  
**Дата:** 2024